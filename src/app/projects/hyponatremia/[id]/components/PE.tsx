import React from 'react';
import { SheetJson } from '../../ram_db/types';
import * as enums from '../../ram_db/enums';

// components
import { StyledTitle, StyledBox } from './StyledComponents';

const PE = (props: {data: SheetJson}) => {
  return (
    <div>
      
    </div>
  )
}

export default PE
